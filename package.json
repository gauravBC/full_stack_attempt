{
  "name": "project",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/dom": "^10.4.1",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "@testing-library/user-event": "^13.5.0",
    "react": "^19.2.5",
    "react-dom": "^19.2.5",
    "react-scripts": "5.0.1",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "api:dev": "cd backend && mvn spring-boot:run",
    "api:test": "cd backend && mvn test",
    "db:up": "docker compose up -d postgres redis kafka",
    "mobile:start": "cd mobile && npm start",
    "api:dev:openai": "AI_PROVIDER=openai SERVER_PORT=8081 DATABASE_URL=jdbc:postgresql://localhost:5433/nurtureai npm run api:dev",
    "api:dev:ollama": "AI_PROVIDER=ollama OLLAMA_MODEL=llama3.2 SERVER_PORT=8081 DATABASE_URL=jdbc:postgresql://localhost:5433/nurtureai npm run api:dev"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
